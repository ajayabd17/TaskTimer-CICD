apiVersion: apps/v1
kind: Deployment
metadata:
  name: tasktimer-green
spec:
  replicas: 2
  selector:
    matchLabels:
      app: tasktimer
      version: green
  template:
    metadata:
      labels:
        app: tasktimer
        version: green
    spec:
      containers:
        - name: tasktimer
          image: ajayabd17/tasktimer-react:latest
          ports:
            - containerPort: 80
